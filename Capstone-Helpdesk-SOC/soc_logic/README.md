README

AI is able to injest a json file and categorize it.

Current Plans as of 3/4/26
-Fix percentage calculations
-Apply encryption/TLS implementation
-Add a try/except to alert SOC if AI fails to parse data,
(ex try: ..input into ai.... if error then print("AI failed sent to 
manual review. This aligns with regulations.))

later
-After setting up virtual lab test to see if other
systems will have their logs parsed from a 
new json file created in real-time.
-Implement into wahzuh along with IT